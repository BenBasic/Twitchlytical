import React from 'react'

/* Make dropdown of categories, will map from the api a list of categories
would probably be best to treat tags as categories (ex: shooter, irl, rpg, etc)
and it can show top games or top streamers for that category.
Top 3 could be a row of TopCards and the rest could display as a list
*/

const TopByCategory: React.FC = () => {
  return (
    <div>TopByCategory</div>
  )
}

export default TopByCategory